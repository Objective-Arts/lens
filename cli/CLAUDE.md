## Profiles Applied

`frontend`

## Auto-Invoke Skills

| Context | Action |
|---------|--------|
| **System/architecture design** | **INVOKE `/taleb` (antifragility) then `/petroski` (failure learning)** |
| **Major refactoring** | **INVOKE `/petroski` then `/taleb`** |
| Building UI component | INVOKE `/frost` then `/ive` |
| Designing forms | INVOKE `/wroblewski` then `/norman` |
| Adding animation | INVOKE `/duarte` |
| Mobile design | INVOKE `/wroblewski` then `/buxton` |
| CSS styling, layouts | INVOKE `/rams` for simplicity check |
| **Feature implementation complete** | **INVOKE `/dodds` (tests) then `/procida` (docs) then `/review-hard`** |

## Anti-Patterns (NEVER DO)

| Anti-Pattern | Why It's Wrong |
|--------------|----------------|
| Marking items complete without documentation | Code without docs is incomplete code |
| Skipping tests for "simple" code | Simple code breaks too; tests catch regressions |
| Saying "tests not needed here" | All JS/TS code needs tests per Testing Trophy |
| Completing PRD items without running /review-hard | Quality gates exist for a reason |
