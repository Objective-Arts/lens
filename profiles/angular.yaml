name: angular
description: Angular frontend. Combine with javascript profile.
projectType: software
composable: true
extends: javascript

ralph:
  skills:
    plan:
      - hevery         # DI architecture
      - minko-gechev   # Angular patterns
    build:
      - hevery         # Angular core
      - ben-lesh       # RxJS
      - minko-gechev   # Performance
      - kurata         # Angular best practices
    refactor:
      - minko-gechev   # Angular optimization
      - ben-lesh       # RxJS refactoring
    test:
      - hevery         # Angular testing

skills:
  canon:
    - hevery         # Angular creator - DI, testability, component design
    - kurata         # Angular architecture, module organization, state patterns
    - minko-gechev   # Performance - lazy loading, OnPush, bundle optimization
    - ben-lesh       # RxJS patterns - streams, operators, subscription management

agents:
  - css-expert
  - accessibility-tester

claudeMd:
  standards:
    - "Use OnPush change detection strategy"
    - "Prefer signals over BehaviorSubject for state"
    - "Use standalone components (not NgModules)"
    - "Inject services in constructors, not property injection"
    - "Use trackBy with ngFor"
    - "Lazy load feature routes"
    - "Use async pipe for observables in templates"
    - "Keep components small and focused"

  antiPatterns:
    - "Default change detection for performance-critical components"
    - "Manual subscription management (use async pipe)"
    - "Business logic in components (use services)"
    - "Direct DOM manipulation"
    - "Large shared modules"
    - "Circular dependencies between services"
    - "Not unsubscribing from long-lived observables"

  autoInvoke:
    - context: Angular DI, testability, component architecture
      action: INVOKE `/hevery`
    - context: Module organization, feature structure, state management
      action: INVOKE `/kurata`
    - context: Performance, lazy loading, change detection, bundle size
      action: INVOKE `/minko-gechev`
    - context: RxJS, observables, streams, async composition
      action: INVOKE `/ben-lesh`
