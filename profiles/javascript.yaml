name: javascript
description: JavaScript/TypeScript expertise - combine with base-tech
projectType: software
composable: true
extends: software-base

skills:
  canon:
    # JavaScript experts
    - kyle-simpson   # You Don't Know JS - closures, scope, this
    - abramov        # Redux, React patterns, composition
    - ashkenas       # Underscore/Backbone, functional JS
    - cherny         # Programming TypeScript - type system mastery
    - crockford      # JavaScript: The Good Parts - language subset
    - dodds          # Testing Trophy, React testing
    - harris         # Svelte, reactive programming
    - osmani         # Learning JavaScript Design Patterns
    # UI/UX experts (frontend JS needs these)
    - frost          # Atomic Design - components
    - ive            # Visual design - minimalism
    - norman         # UX psychology - affordances
    - wroblewski     # Mobile-first, forms
    - duarte         # Motion design, animation
    - buxton         # Input design, sketching
    - curtis         # Design systems, tokens
    - kruzeniski     # Typography
    - rams           # 10 principles - simplicity

claudeMd:
  standards:
    - "Use const by default, let when reassignment needed"
    - "Prefer arrow functions for callbacks"
    - "Use async/await over raw promises"
    - "Destructure objects and arrays"
    - "Use template literals for string interpolation"
    - "Prefer named exports over default exports"
    - "Use TypeScript strict mode"
    - "Avoid any type - prefer unknown when type is uncertain"

  antiPatterns:
    - "var declarations (use const/let)"
    - "Implicit type coercion (use === and !==)"
    - "Global variables"
    - "Callback hell (use async/await)"
    - "Mutating function parameters"
    - "Using any type without justification"
    - "Ignoring promise rejections"

  autoInvoke:
    # JavaScript
    - context: Complex JavaScript runtime, closures, this, scope
      action: INVOKE `/kyle-simpson`
    - context: TypeScript types, generics, inference, utility types
      action: INVOKE `/cherny`
    - context: JavaScript subset, safe patterns, avoiding pitfalls
      action: INVOKE `/crockford`
    - context: React testing, Testing Library, Testing Trophy
      action: INVOKE `/dodds`
    - context: Redux, React state, composition patterns
      action: INVOKE `/abramov`
    - context: Functional JavaScript, lodash patterns
      action: INVOKE `/ashkenas`
    - context: Svelte, reactive frameworks
      action: INVOKE `/harris`
    - context: JavaScript design patterns, module patterns
      action: INVOKE `/osmani`
    # UI/UX (always invoke for frontend work)
    - context: Building UI components, React components, frontend
      action: INVOKE `/frost` then `/ive` then `/norman`
    - context: Forms, inputs, validation UI
      action: INVOKE `/wroblewski` then `/norman`
    - context: Animations, transitions, motion
      action: INVOKE `/duarte`
    - context: Mobile design, responsive, touch
      action: INVOKE `/wroblewski` then `/buxton`
    - context: Design system, tokens, theming
      action: INVOKE `/curtis`
    - context: Typography, text styling, fonts
      action: INVOKE `/kruzeniski`
    - context: CSS, styling, layouts
      action: INVOKE `/rams`
